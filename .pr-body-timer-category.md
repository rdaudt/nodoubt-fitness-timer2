## Summary
- add workout category domain (`GENERAL`, `FAT-LOSS`, `PERFORMANCE`) with `GENERAL` default
- show category on home timer cards, timer editor, running page, and run history
- add home filter (`ALL`, `GENERAL`, `FAT-LOSS`, `PERFORMANCE`) defaulting to `ALL`
- allow editing category in timer editor and run history
- include timer category in export/import flows with normalization fallback for legacy data
- align category badge on home card to top-right card border
- render workout category dropdown in a single row on home and timer editor

## Validation
- `npm run build`
- `npx vitest run src/services/storage.test.ts src/services/timerTransfer.test.ts src/components/TimerCard.test.tsx src/pages/TimerDetailPage.test.tsx`
- `npx vitest run src/pages/HistoryPage.test.tsx src/pages/RunningTimerPage.test.tsx`
